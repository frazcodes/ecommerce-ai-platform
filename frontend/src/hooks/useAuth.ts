/**
 * useAuth Hook
 * 
 * Purpose: Convenience hook for accessing auth context
 * Why it exists: Re-exports useAuth from AuthContext for cleaner imports
 * Features: Type-safe auth context access
 */

export { useAuth } from '../contexts/AuthContext'
